// Google Consent Management Platform (CMP) configuration
export const CMP_CONFIG = {
  enabled: true,
  // CMP is integrated in index.html with Google's certified CMP
  // Provides GDPR consent management for EU/UK/CH users
};